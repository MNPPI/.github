name: Quality

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    name: Public Quality
    uses: ./.github/workflows/public-quality.yml
    with:
      profile: metadata
